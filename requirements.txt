fastapi>=0.115.0
uvicorn[standard]>=0.32.0
pydantic>=2.0.0
pydantic-settings>=2.0.0
httpx>=0.27.0
python-dotenv>=1.0.0
PyYAML>=6.0
unidecode>=1.3.0
sqlalchemy[asyncio]>=2.0
asyncpg
aiosqlite
alembic
redis>=5.0

# Las dependencias de entrenamiento ML viven en requirements-ml.txt.
# Están separadas a propósito: torch+transformers pesan ~2GB y romperían
# el cold start en producción.
# En producción, cuando exista el detector (Fase 3.3), solo se agregarán
# onnxruntime y tokenizers.
